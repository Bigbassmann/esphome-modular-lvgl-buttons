# FILE EDIT GUIDE (SenseCAP)
# Role: Reusable button template (structure + behavior hooks).
# Update here for:
# - Size/shape defaults: width/height/radius/padding/alignment.
# - Show/hide behavior flags for icon/text/value/state widgets.
# - Runtime logic: on_press, on_value, lambda/then blocks, intervals.
# Keep entity wiring out of this file:
# - Entity/attribute mapping belongs in common/ha_entities-*.yaml.
# - Page placement/layout belongs in pages/*-sensecap*.yaml.
# - Color defaults should resolve via shared theme styles/tokens first, with per-page overrides only when needed.
---
# Non-clickable placeholder tile
# vars:
#    uid:
#    page_id:
#    row:
#    column:
#    row_span:
#    column_span:
#    text:
#    icon:
#    bg_color: (optional)
#    icon_color: (optional)
#    text_color: (optional)

lvgl:
  pages:
    - id: !extend ${page_id | default("main_page")}
      widgets:
        - container:
            id: ${uid}
            grid_cell_row_pos: ${row}
            grid_cell_column_pos: ${column}
            grid_cell_row_span: ${row_span | default(1)}
            grid_cell_column_span: ${column_span | default(1)}
            grid_cell_x_align: stretch
            grid_cell_y_align: stretch
            widgets:
              - button:
                  id: button_${uid}
                  clickable: false
                  styles: sense_tile_placeholder_style
                  bg_color: ${bg_color | default("theme_placeholder_bg_default")}
                  widgets:
                    - label:
                        id: icon_${uid}
                        text_font: $icon_font
                        styles: sense_text_muted_style
                        align: top_left
                        x: 10
                        y: 8
                        text: ${icon}
                        text_color: ${icon_color | default("${ui_state_text_inactive_color}")}
                    - label:
                        id: label_${uid}
                        align: left_mid
                        x: 42
                        text_font: nunito_16
                        styles: sense_text_muted_style
                        text: ${text}
                        text_color: ${text_color | default("${ui_state_text_inactive_color}")}





