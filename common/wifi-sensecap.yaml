---
wifi:
  
  ssid: ${wifi_ssid}    # !secret wifi_ssid
  password: ${wifi_password}    # !secret wifi_password
  fast_connect: true
  min_auth_mode: WPA2
  ap:
#    password: "password"
    ssid: ${ap_ssid}
    password: ${ap_password}

captive_portal:

web_server:
  port: 80
  version: 3

improv_serial:

# esp32_improv:
#   authorizer: none
